List<Navigation> navigations = [
  Navigation(name: "Bill-Payments-Screen-UI", desc: ""),
];

class Navigation {
  String name;
  String desc;
  Navigation({
    required this.name,
    required this.desc,
  });
}
